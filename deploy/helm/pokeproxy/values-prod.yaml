hmac:
  encryptedValue: CHANGEME

ingress:
  enabled: true
  className: nginx
  bodyLimit:
    enabled: true
    maxBytes: 1048576
    provider: nginx

networkPolicy:
  enabled: true

components:
  pokeproxy:
    image:
      repository: ghcr.io/CHANGEME/pokeproxy
      tag: CHANGEME

  mock-downstream:
    enabled: false
